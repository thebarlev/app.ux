# חמשת כללי ההתנהגות של מסך הסריקה

> **סטטוס: מקור יחיד ומחייב.**
> הכללים האלה נאמרו בעל-פה לסוכן קודם ומעולם לא נשמרו לקובץ — ולכן אבדו כשהסוכן הוחלף.
> הקובץ הזה קיים כדי שזה לא יקרה שוב. אין מסמך שני להשוות אליו. אם מישהו טוען שיש —
> אין. זה המקור.
>
> נשמר: 2026-07-29 · הקשר: פורט מסך הסריקה + שער הליד לפי
> `design-mockups/auditor-scanflow-v3-light-FINAL.html`
> (sha256 `3a130eea396deed08af5b1245ebe65229f15363ee5e1501569698cea86a114ae`).

הכללים בלתי-נראים למשתמש. הם לא פיצ'רים ולא קישוט — הם ההבדל בין מסך שמדווח על
מה שקורה באמת לבין אנימציה שמעמידה פנים. המוקאפ הוא מפרט תצוגה בלבד; ה-BEATS שבו
הם ציר זמן מבוים של ~5.5 שניות ואסור להעתיק אותו כמו שהוא. הכללים האלה גוברים על
המוקאפ בכל מקום שבו הם מתנגשים.

---

## 1 · רק התקדמות אמיתית

האחוז נגזר מהשלב שהפייפליין נמצא בו בפועל. לעולם לא מגיע ל-100% לפני שהציון קיים
באמת. אין השלמה מלאכותית ואין "כמעט סיימנו".

## 2 · נגד קיפאון, עם תקרה

בין שתי נקודות בידוק אמיתיות המספר ממשיך לטפס באינטרפולציה מרוסנת שנחלשת עם הזמן —
אבל לעולם לא חוצה את ערך נקודת הבידוק הבאה. שלב שנמשך — המספר מאט ונעצר מתחת
לתקרה. לא קופץ קדימה.

## 3 · מספרים לא יורדים

אף אחוז ואף מונה לא יורד אחורה, גם אם השרת החזיר ערך נמוך יותר בגלל רשת, מרוץ או
ריענון. תמיד `max(current, incoming)`.

## 4 · תנועה מופחתת

`prefers-reduced-motion: reduce` מכבה לייזר, רקע זוחל, פעימה וסטגר; המספר נקבע
ישירות. הסריקה מתקדמת ומגיעה לשער בדיוק כמו במצב רגיל.

## 5 · השער נפתח רק על אמת

השער נפתח רק כשהציון קיים בפועל. כישלון סריקה מציג מצב כישלון מפורש — לא שער ולא
דוח ריק. שום טקסט על המסך לא מבטיח דבר שאין מאחוריו מימוש חי.

---

## החלטות יישום שנגזרו מהכללים (סבב 2026-07-29)

ההחלטות האלה אינן הכללים עצמם — הן התוצאה של החלתם על המוקאפ. נשמרות כאן כי כל אחת
מהן היא מקום שבו המוקאפ הראה משהו שאין מאחוריו נתון.

| נושא | החלטה | הנימוק העובדתי |
|---|---|---|
| ריבוע האתר | מצב ריק מעוצב + לייזר, בגודל מלא. ללא `<iframe srcdoc>` וללא תמונה. | `AUDITOR_SCREENSHOT_ENABLED=""` בפרודקשן → `artifacts.screenshot_url` לא נכתב, `screenshot_url` תמיד `null`. הדגל כובה בכוונה (`continue.ts:338-341`) כי Chromium היה הגורם המוביל לתקיעות סריקה. ה-HTML הגולמי לא זמין בצד לקוח. |
| פינים | ירדו לגמרי מהפלואו הציבורי. | 3 מ-4 הפינים במוקאפ הם pass/info. הפלואו הציבורי חושף רק `fail`/`warn` דרך `issues_overview` — אין passes. ארבעה מיקומים קבועים עם אחד מאויש גרוע מאפס. |
| ביט המתחרים | ירד. וגם `#3 מול מתחרים` ב-peek של השער וב-אריח הדוח. | `AUDITOR_SERPER_API_KEY` לא קיים ב-env של פרודקשן. `competitors.ts:68-69` מחזיר `[]` בלי מפתח. מספר מומצא על המסך הוא שקר ללקוח. |
| מונים | שניים: "עמודים נסרקו", "ממצאים". | `pages_scanned` ו-`issues_count` מוחזרים אמיתיים מ-`/api/auditor/status`. "בדיקות בוצעו" ירד: כל החוקים נכנסים בבאץ' אחד בשלב `rules` — 0→22 בקפיצה זו הודעה, לא מונה. ש"ממצאים" יעמוד על 0 עד `rules` זו האמת. |
| דלתא לכל ממצא (`+8`/`−21`) | לא מוצגת. | מנוע הניקוד אינו אדיטיבי: `runner.ts:39-46` מחשב `Σ(status×weight)/Σweight × משקל-קטגוריה`. ה-`weight` הוא משקל יחסי בקטגוריה, לא נקודות. תרומה שולית נגזרת רק אחרי שכל חוקי הקטגוריה ידועים, וה-endpoint הציבורי לא חושף weight או category. |
| לוג | שתי שכבות: מעברי שלב אמיתיים לאורך הסריקה, ממצאים אמיתיים בשלב `rules`. ניטרלי לשלבים, אדום/כתום לממצאים. **אין ירוק.** | אין passes בפלואו הציבורי, ולכן אין מאיפה לייצר שורה ירוקה. |
| פריסת ~80ms בין שורות ממצא | מותרת. | החלטת תצוגה בלבד, לא טענה על זמן גילוי. הנתונים אמיתיים ומגיעים באותו רגע; הפריסה קיימת רק כדי שקיר שורות לא יופיע בפריים אחד. לעולם לא נמשכת מעבר לשלב עצמו, וכלל 4 מכבה אותה. |

## הכרעות סבב 2

שש שאלות שעלו במימוש והוכרעו ב-2026-07-29. רשומות כאן כדי שלא יתגלו מחדש.

### 1 · השער לא נפתח לפני `score_ready` — כלל 5 עומד

הטיימר של 5 שניות שהיה כאן קודם בוטל. השער מצהיר "הדוח מוכן" ומצהיר מספר עמודים
ומספר ממצאים; לפתוח אותו לפני שיש ציון זו הבטחה שאין מאחוריה כלום. **ליד שנתפס
בהבטחה כזו גרוע מליד שאבד.**

המחיר מודע: באתר איטי הטופס מופיע מאוחר יותר, וחלק מהמבקרים יעזבו לפני.

**התקרה — יש, והיא בקוד.** "להמתין לאמת" אינו "להמתין לנצח":

- `lib/auditor/pipeline/continue.ts:453` — `GLOBAL_SCAN_TIMEOUT_MS`, **10 דקות**
  לסריקה רגילה (5 דקות ל-verification).
- התקרה נבדקת בכל קריאה ל-`/api/auditor/continue`, ולא ברקע. הלקוח קורא ל-continue
  כל 1200ms כל עוד הסטטוס `queued` או `running` — ואלה הסטטוסים היחידים שקיימים
  לפני סיום (`pre-scan` יוצר `queued`; continue כותב `running`, ואז `done` או
  `failed`). אין ערך סטטוס שנופל בין הכיסאות, ולכן אין מסלול שבו התקרה לא נבדקת.
- בתקרה רץ `finalizeScan(reason:"global_timeout")` ומחזיר בדיוק אחת משתי מצבים
  סופיים: `done` **עם** `score_total` סופי → `score_ready` → השער נפתח באמת; או
  `failed` → מצב הכישלון המפורש, בלי שער.
- `assertScoredDone` (שורה 200) אוכף את האינוריאנטה: patch שכותב `status:"done"`
  חייב לשאת `score_total` סופי, אחרת הוא נדחה ומורד ל-`failed`. כלומר **"done בלי
  ציון" אינו מצב אפשרי מבנית.**

**מסקנה: אין מסלול המתנה אינסופי.** הרע ביותר למבקר הוא 10 דקות על מסך הסריקה,
ואז שער אמיתי או כישלון מפורש — שניהם מצבים מוגדרים.

**מה שכן פתוח לסבב הבא (UX, לא נכונות):** 10 דקות מול מסך סריקה הן חוויה גרועה גם
כשהיא כנה, ובגלל כלל 2 המספר יעמוד כמעט בלי לזוז ברובן. שווה להחליט מה המבקר רואה
אחרי ~45–60 שניות ("זה לוקח יותר מהרגיל") — אמירה כזו כנה ואינה נוגדת אף כלל.

### 2 · תיבות ההסכמה נשארות לא מסומנות

במוקאפ שתיהן `checked`. זו הייתה שגיאה במוקאפ. תיבה מסומנת מראש אינה הסכמה. זה
תיקון, לא סטייה.

### 3 · אריח הציון בשער הוא בלוק ריק

המוקאפ מציג `<b>87</b>` תחת `filter: blur(5.5px)`. blur אינו גבול אבטחה — המספר
נשאר ב-DOM וקריא ב-devtools, מה שהופך את הטופס שהוא אמור לגדר לאופציונלי. הערך לא
מטושטש, הוא **לא מרונדר**. מונה הממצאים כן מוצג, כי הכותרת מעליו כבר אומרת אותו
במילים.

### 4 · `AiScoreHero.tsx` — למחוק, בקומיט נפרד, בסבב הבא

קוד מת (מוגדר, לא מיובא לשום מקום) שמכיל את המופע האחרון של טענת המתחרים:
"נוכחות AI מינימלית — המתחרים שלכם כבר שם". קוד מת שמכיל טענה מפוברקת הוא מוקש למי
שייבא אותו יום אחד. **לא נמחק באמצע בנייה** — קומיט נפרד ומסומן.

### 5 · צעדי המתחרים שקופים בלוג

`competitor_discovery` / `competitor_crawl` / `competitor_keywords` באמת רצים,
והאחוז מתקדם דרכם. הם לא כותבים שורת לוג, כי בלי מפתח SERP הם לא מייצרים דבר
למבקר הזה. **שתיקה כנה עדיפה על שורת לוג ממציאה.**

### 6 · גוון הממצאים — ענבר, לא אדום

`issues_overview` הוא רשימת מחרוזות שטוחה שמערבבת `fail` ו-`warn` בלי סימון, ולכן
חומרה אינה נגזרת בפלואו הציבורי. אדום מכריז "קריטי" על מה שאולי אזהרה; ירוק היה
עושה את ההפך. גוון אחד ניטרלי-קשוב — `#B7791F` על `#FDF3E3` — והכותרת **"ממצאים"**
ולא "בעיות". לא ממציאים חומרה לשום כיוון.

> נגזרת שלא טופלה: `AuditorReportV3` עדיין מכנה את אותו ערבוב `fail`/`warn`
> "בעיות שנמצאו" / "ועוד N בעיות — בפרימיום". אותו ליקוי בדיוק, במסך 3. לא שונה
> כאן כדי לא לגעת במסך הדוח באמצע סגירה — פריט לסבב הבא.

## היקף הסריקה: למה `page_limit` אינו הידית שנראה שהוא

נבדק ב-2026-07-29 אחרי ששער הליד הציג "סרקנו 1 עמודים ומצאנו 6 ממצאים". התגובה
האינטואיטיבית — להעלות את `page_limit` — **לא תעבוד**, ושווה שזה יהיה כתוב לפני
שמישהו ינסה שוב.

### מספר הממצאים אינו תלוי במספר העמודים

במנוע יש **22 חוקים** (`results.push` ב-`lib/auditor/rules/definitions.ts`).
`ctx.pages` מופיע שם **בשני מקומות בלבד**:

- שורה 38 — `homepage()`, שבוחר את עמוד השורש
- שורה 373 — `schema.faq_or_article`, שבודק `ctx.pages.some(...)` עבור
  FAQPage/Article

כלומר **21 מתוך 22 החוקים מנקדים את עמוד הבית בלבד.** תקרת הממצאים היא 22 — מספר
החוקים — ולא מספר העמודים.

וכיוון ההשפעה של החוק היחיד שכן קורא עמודים נוספים הוא **הפוך** מהצפוי: יותר
עמודים יכולים רק להפוך אותו מ-fail ל-pass, כלומר **פחות ממצאים וציון מעט גבוה**.
סריקת 5 עמודים במקום 1 לא תוסיף ולו ממצא אחד.

"סרקנו 1 עמודים ומצאנו 6 ממצאים" היה מדויק: 22 חוקים רצו על עמוד אחד, 6 סימנו.

### ומה בכל זאת מגביל לעמוד אחד

לא העמודה. `pre-scan` (הזרימה הציבורית) יוצר סריקות עם
`scan_kind: "verification"`, ו-`continue.ts:480` קובע
`pageLimit = isVerification ? 1 : (column ?? 10)` — **העמודה מתעלמים ממנה
לחלוטין** בענף הזה. גם `page_limit: 20` בשורה היה מניב עמוד אחד.

`scan_kind` אינו דגל תמים; הוא מפעיל חמישה שינויים בבת אחת: `pageLimit` (1 מול
העמודה), `batchSize` (1 מול 5), `normalizeNextStep` (`sample` מול `robots` —
מדלג על robots/sitemap/ai_files), תקרת זמן (300s מול 600s), ומסלול ניקוד ייעודי
ב-`finalizeScan`.

### מה זה אומר מעשית

| שינוי | סיווג | תוצאה |
|---|---|---|
| `page_limit` ב-`pre-scan` | קונפיגורציה | מותר, **ואפס אפקט** |
| `scan_kind` ב-`pre-scan` | ערך בשורה שמפעיל ענף פייפליין | חריגה — דורש אישור מפורש |
| `isVerification ? 1 : …` ב-`continue.ts:480` | קוד פייפליין | **קו אדום** |

זמנים מדודים (16 סריקות שהסתיימו, 78 משיכות עמוד): `verification` בעמוד אחד —
חציון **5.3 שניות**; `initial` בעשרה עמודים — חציון **97.3 שניות**. משיכת עמוד
בודד היא ~1.1s בלבד, כך שרוב הפער הוא תקורת ארכיטקטורה (`batchSize=1` פירושו
עמוד לכל tick), לא רשת. מעבר ל-`initial` יחצה את **שני** ספי הסריקה הארוכה.

### הידית האמיתית, אם אי-פעם תידרש

כיסוי אמיתי מחייב **חוקים שרצים פר-עמוד** במנוע הניקוד — פרויקט בפני עצמו, לא
שינוי פרמטר. עד אז, הרחבת `page_limit` היא עלות בלי תמורה.

ההחלטה שהתקבלה בסבב הזה: **טקסט בלבד.** השער אומר "בדקנו את עמוד הבית" במקום
לספור עד אחד. ראו `a47e17e`.

## מקורות נתונים אמיתיים (`GET /api/auditor/status`)

מה שהפלואו הציבורי מקבל בפועל — כל דבר שאינו ברשימה הזו אינו קיים בצד לקוח:

- `status`, `step` — שלב חי מהפייפליין
- `progress` — נגזר מ-`step` דרך `computeProgress` (מדרגות, לא רציף)
- `pages_total`, `pages_success`, `pages_failed`, `pages_scanned` — **ספירות בלבד**
- `issues_overview` (`fail`/`warn` בלבד), `issues_count`
- `score_ready`, `score_total` — `score_ready` הוא בדיוק תנאי כלל 5
- `screenshot_url` — תמיד `null` בפרודקשן
- `hostname`, `confidence_level`, `warning`, `done`

**לא מוחזר:** נתיבי/URL-ים של עמודים (`auditor_scan_pages` נשלף עם `.select("state")`
בלבד), סטטוסים לכל חוק, משקלים, קטגוריות, נתוני מתחרים.
