# צ׳קליסט הגשה לרשם בתי התוכנה

עודכן: 13.8.2026 · כל עבודת הרגולציה על `regulatory/bkmv-spec`, אפס מיזוגים ל-main
מטרה יחידה: להגיש את `app.ux` ולקבל תעודת רישום. בלי זה החברה לא יכולה
להפיק מסמכים כדין.

כלל מיון: כל דבר שלא מקרב את ההגשה — יורד לרשימה הקפואה. בלי יוצאים מן הכלל.

## כללי עבודה שעומדים בתוקף

- לא ממזגים ולא פורסים בלי אישור מפורש.
- לא מריצים SQL שמשנה נתונים בפרודקשן. הסוכן מחבר, איציק מריץ ומחזיר פלט.
- לא נוגעים ב-`.env` ולא מדפיסים ערכי משתני סביבה.
- לא עורכים מיגרציות קיימות. מיגרציה חדשה בסוף הרצף בלבד.
- לא `git add .` ולא `git commit -a`. נתיבים מפורשים בלבד.
- לפני כל הרצת SQL — להצהיר באיזה פרויקט Supabase נמצאים.
- "הושלם" בלי פלט אמיתי אינו הושלם. אין וי על סמך קריאת קוד.
- `/api/auditor/billing/renewals/run` לא מחובר לסקדיולר עד לתיקון
  באג הבעלות ב-`_impl`.

---

## שלב 1 — המערכת החשבונאית עובדת רגולטורית

### 1.0 בונה הקובץ האחיד ⛔ החוסם הראשון

**הסתירה נפתרה 13.8.2026: יש שני בונים.**

הבונה שעל `main` הוא שלד:

- `assertBkmvSpecComplete()` בשורה 34 זורק לפני כל דבר אחר
- כל שמונת סוגי הרשומות מוגדרים `recordCodeOnly` — שדה אחד, `record_code`
- `lineItems` מופיע בחתימה ומעולם לא נקרא
- D110 = רשומה אחת למסמך עם `total_amount` של הכותרת, לא רשומת פריט
- D120 נוצר רק כש-`documentType === "receipt"`. `invoice_receipt` לא נכנס.
- ⚠️ והראוט `app/api/regulatory/bkmv/export/route.ts:139` **כן מחובר אליו
  ופרוס בפרודקשן** — כלומר ייצור קובץ אחיד מהאתר החי נכשל תמיד.

**הבונה האמיתי הוא על הענף `regulatory/bkmv-spec`** — 14 קבצים בספרייה,
כולל `map.ts`, `ini.ts`, `encoding.ts`, `text.ts`, `fields.ts`, `codes.ts`,
ו-`docs/regulatory/bkmv/fields-1.31.json` (2,187 שורות, טבלאות שדות עם
`from`/`to`/`len`/`req`), ושלושה קבצי טסט.

**איך נקבע:** שגיאות הסימולטור היו ברמת שדה — 1225, 1006, 1217, 1025.
המחרוזות `1217` ו-`1225` **אינן קיימות בשום קובץ ב-`main`**, ואינן יכולות
היו להיווצר שם. הן קיימות ב-`map.ts` שעל הענף השני. אותו דבר ל-`&OF1.31&`
ול-`ISO-8859-8`: אפס ב-`main`, קיימים בענף.

- [x] לאתר מה בפועל הפיק את BKMVDATA.TXT שהרצנו בסימולטור — `regulatory/bkmv-spec`
- [x] לקבוע איזה בונה הוא הבונה האמיתי — הענף, לא `main`
- [x] **טבלאות שדות באורך קבוע** — `fields-1.31.json`, 2,187 שורות. אומת בפלט:
      `A000 466 · A100 95 · C100 444 · D110 339 · D120 222 · Z900 110`, כולם תואמים למפורסם
- [x] **D110 נבנה משורות פריט אמיתיות** — אומת בבייטים: D110 של 1157 נושא
      `מיאושי - עולם הזוגיות`, כמות `0001000`, לא סכום הכותרת
- [x] **D120 נוצר גם ל-`invoice_receipt`** — 4 רשומות D120 על 1004–1007 (קוד 320)
- [x] **INI.TXT — סיכומי רשומות תואמים** — `C100 8 · D110 8 · D120 4` מול BKMVDATA
- [x] **קידוד ISO-8859-8-i, CRLF, `&OF1.31&`** — הקבצים נפתחו ונקראו נקי
- [x] **סעיף 2.6 ונספח 4 מצוטטים במלואם** מעמודים 9 ו-20 של המפרט
- [ ] הכרעה מה קורה עם שני הבונים — **החלטת איציק, לא איחוד יזום**
      `main` מריץ שלד שזורק `BKMV_SPEC_INCOMPLETE`, והראוט שלו פרוס

### 1.1 שורת תקבול ואמצעי תשלום (D120)

5 מתוך 9 מסמכי `invoice_receipt` בספרים בלי שורת תשלום, וגם קבלה 4000.
הנתיב הידני לא יוצר שורת תשלום. הטופס לא שואל על אמצעי תשלום כלל.

| מסמך | סוג | item | payment | other |
|---|---|---|---|---|
| 1000 | invoice_receipt | 0 | 0 | 1 |
| 1001 | invoice_receipt | 0 | 0 | 1 |
| 1002 | invoice_receipt | 0 | 0 | 1 |
| 1003 | invoice_receipt | 0 | 0 | 1 |
| 1004 | invoice_receipt | 0 | 1 | 1 |
| 1005 | invoice_receipt | 0 | 1 | 1 |
| 1006 | invoice_receipt | 0 | 1 | 1 |
| 1007 | invoice_receipt | 0 | 1 | 1 |
| 1157 | invoice_receipt | 0 | 0 | 1 |
| 4000 | receipt | 0 | 0 | 1 |

מיגרציה 136 היא קו פרשת המים. הנתיב הידני מעולם לא קיבל את התיקון המקביל.
`kind='item'` הוא אפס בכל המסד.

- [x] **רכיב התשלומים כבר ב-`InvoiceReceiptFormClient`** — `:33` מייבא את אותו
      `PaymentDetailsSection`, מרנדר ב-2046 וב-2265, מחזיק `payments: PaymentRow[]`
      ושולח `payments: payments` במטען שנשמר. **הדיווח הקודם שלי היה שגוי.**
- [x] ⛔ **הפער היה בכתיבה, וגרוע יותר** (`2f135f8`) — שלושת אתרי ההוספה כתבו
      `if (items) … else if (payments) …`, ו-`isItemDocumentType("invoiceReceipt")` הוא
      true. כל 320 עם שורות פריט — כלומר כולם — נכנס לענף הראשון ושורת התשלום נזרקה
      בשקט. אומת בבייטים: 6 רשומות D120 בקובץ הדגמה.
- [x] **`payment_metadata.kind` נכתב במפורש** — בלעדיו `classifyLine` היה קורא שורת
      תשלום על 320 כסחורה, כלומר D110 במקום D120
- [x] **גם הקריאה תוקנה** — שני ה-loaders מיפו כל שורה לשני המערכים
- [ ] ~~העברת הרכיב~~ — מבוטל: הרכיב שם
      ⛔ לא שדה חדש. המבנה קיים ועובד: `payments: [{ method, date, amount, currency }]`,
      ומייצר D120 תקין על קבלה 4000. אותו רכיב, אותו מבנה, אותה כתיבה —
      מבנה שני לאותו מושג יתפצל ברגע שאחד מהם ייגע.
- [ ] שורת תשלום עם `kind='payment'` ובסכום **ברוטו** — מה ששולם בפועל, כמו ב-137
- [ ] אימות: מסמך חדש מייצר D120 תקין בקובץ האחיד

**⛔ למה זה חזר לעבודה אחרי שהורד:** "D110 ו/או D120" עונה על תקינות הפורמט וזה
נכון ולא רלוונטי. חשבונית מס/קבלה **היא** קבלה — היא מתעדת כסף שהתקבל. קובץ שבו
כל מסמכי 320 אינם נושאים ראיה למה שהתקבל מציג תוכנה שלא יודעת לתעד תשלום במסמך
המרכזי שלה. עובר את הפורמט, נכשל בעניין.

**והציטוט מכריע:** נספח 1, עמוד 18, שורת 320 היא `חשבונית מס / קבלה` **ותו לא**.
`grep` על `"ללא פרטי הקבלה"` בכל 22 העמודים — **אפס**. ההסתייגות ההיא ממפרט ה-QR
ואינה קיימת במפרט הקובץ האחיד, כלומר אין בסיס לקרוא 320 כ"בלי פרטי קבלה".

ברירת מחדל "כרטיס אשראי" פסולה — היא רושמת אמצעי תשלום כוזב במסמך מס.

### 1.2 חשבונית זיכוי

- [ ] קוד מסמך 330 בנספח 1
- [ ] סכומים חיוביים — הזיכוי מבוטא בקוד המסמך, לא בסימן
- [ ] שדות 1256/1257 (מסמך בסיס) — חובה אצלנו גם אם אופציונליים במפרט
- [ ] "זיכוי מתוך החשבונית" — העתקת תוכן החשבונית לטופס זיכוי, סכום ניתן לעריכה
- [ ] הסרת החסימה הקיימת על הפקת זיכוי
- [ ] 7 השערים

### 1.3 עקביות סכומים בשורה

`TaxInvoiceFormClient.tsx`:

- שורה 646: `unitPrice: item.unitPrice || 0` — ברוטו כפי שהוקלד (37.00)
- שורה 652: `lineTotal: getLineNet(item)` — נטו, מחולק ב-1.18 (31.36)

כמות × מחיר יחידה ≠ סכום שורה כש-`vatMode='included'`.
כותרת המסמך תקינה: 31.36 + 5.64 = 37.00. כלומר `line_total` הנטו הוא הנכון
ו-`unit_price` הוא החריג.

- [x] **הציטוט שמכריע** — מפרט 1.31 עמוד 13: `1267` = `"הכמות בשורה * מחיר ליח' ללא
      מע\"מ בניכוי הנחת השורה"`, ו-`1265` הוא `"מחיר ליחידה ללא מע\"מ"`.
      כלומר `1267 = 1264 × 1265 − 1266`, ושניהם נטו.
- [x] **מיושם ואומת בבייטים** — אבל התיקון היה **שכבה אחת גבוה מדי**, והאצווה הוכיחה זאת:
      `getLineUnitNet` יושב ב-form clients, ולכן קורא שאינו הטופס כתב ברוטו. מסמך 320
      מספר 2 בקובץ נושא `1264=1.0000 · 1265=1180.00 · 1267=1000.00` — שורה שהכפל שלה
      אינו מתקיים. `itemRowToLineItem` **גוזר** עכשיו את 1265 מסך השורה הנטו (`af65d91`),
      כך ש-`1267 = 1264 × 1265` מתקיים מבנייה. כל שורה שהופקה אחרי התיקון מקיימת אותו.
- [~] **נתיב האודיטור** — `scripts/137`, שתי שורות בדיוק מול הגדרת הפרודקשן.
      ⚠️ **טרם רצה.** תרוץ עם המיזוג בסוף.
- [x] **התיקון בזמן כתיבה בלבד** — הרנדרר והמפַּה לא נגועים, אומת ב-diff

### 1.4 זהות לקוח (שדות 1223 / 1225)

על 1157: `customer_name` הוא אימייל, `customer_email` ריק, `customer_tax_id` ריק.
טבלת החברות תקינה — לא התחלפו עמודות. הטופס מילא אימייל לשדה השם.

- [x] **חיבור הנתיב הידני ל-`resolve_customer`** — היה כבר עשוי בענף,
      `actions.ts:1097`, ונכשל-סגור. ⛔ **ב-`main` הוא לא קיים**, וזה מסביר את 1157
      מדידתית: הוא הופק מפרודקשן ולכן `customer_id` שלו null.
- [x] **`1225` מיוצר מהמרשם** — `map.ts:320` קורא `d.customerNumber`.
      אומת בבייטים: מנפיק הבדיקה יצא `1225='1'`, בוגו מדיה 15 רווחים.
- [x] **הציטוט על החובה** — עמוד 12: `"חובה כאשר קוד מסמך בטווח 100-710"`.
      320 ו-330 בטווח, כלומר חובה בכל מסמך שלנו. **זה מה שהופך את 1.4 לחוסם פורמט.**
- [ ] שם לקוח לשדה השם — 1157 נושא אימייל ב-`customer_name`
- [ ] ⏸ שדה אימייל לקוח בטופס — **עבודת מוצר, לרשימה הקפואה.** הטופס לא אוסף
      אימייל בכלל, ולכן `resolve_customer` נופל לח.פ או לשם מדויק. מספיק לנתוני דגמה.

### 1.5 שדה 1006

לשון המפרט: "מספר תעודת הרישום של התוכנה ממנה הופק המסמך.
במידה ולא קיימת תעודת רישום יש לציין 99999999."

- [x] **המפרט אינו נותן ערך** — עמוד 10: `"מספר תעודת הרישום של התוכנה במערכת המס"`,
      חובה, ותו לא. `99999999` הוא **החלטה שלנו**, לא ציטוט. הנימוק: המקום היחיד
      שמטפל במצב "אין תעודה" הוא מפרט ה-QR. הסימולטור יכריע.
- [x] `99999999` **אומת בבייטים** — `1006_registration = [99999999]` ב-A000 של קובץ
      הדגמה, ו-`isSample: false`
- [x] `isSample` נגזר מ-1006 ולא מדגל נפרד — מקובע בטסט על ה**גזירה**, לא על הערך

### 1.6 תאריך המסמך

אפס ולידציה, לקוח ושרת. `grep documentDate app/api` מחזיר אפס תוצאות.
הבדיקה היחידה בטופס (שורה 395) היא ש-`dueDate` לא לפני `documentDate`.
מסמך שהופק ב-10.8 נושא תאריך 30.6 — תקופת דיווח מע״מ אחרת.

- [ ] טווח מותר בטופס (`min` / `max`)
- [ ] ולידציה בשרת
- [ ] חסימת תאריך עתידי

### 1.7 מספר רגולטורי מוקצה בהנפקה בלבד

פתיחת טופס מקצה מספר. טפסים נטושים שורפים מספרים —
מסביר את 50 המספרים החסרים ואת `current_number = 1156` מול מסמך גבוה 1155.

- [ ] הקצאה עוברת לרגע ההנפקה
- [ ] אימות: פתיחת טופס וסגירתו לא מזיזה את הרצף

### 1.8 מקור מול העתק

`original.he.pdf` של 1157 נושא את התווית "העתק נאמן למקור".
מטא-דאטה: `/Creator (Chromium)`, `/Producer pdf-lib` — כלומר `pdf-service`.
`pdf-service:2160` — `storageKey = documents/<id>/${variant}.he.pdf`,
ושם הקובץ `original` אפשרי רק כש-`variant === "original"`,
ובאותו ענף (2148) התווית חייבת להיות "מקור".
מסקנה: `label` מועבר בנפרד מ-`variant` ואחד נדרס.

- [ ] `label` נגזר מ-`variant` באותו ענף
- [ ] `pdf-generator.ts:314` — מטביע "העתק נאמן למקור" תמיד, בלי בדיקת variant

### 1.9 סוגי המסמכים

- [x] **`lib/regulatory/bkmv/appendix1.ts`** — 27 הקודים מעמוד 18, מתומללים
- [x] ⛔ **`406` אינו בנספח 1** — הוא ממפרט ה-QR. הורד מההצהרה.
- [x] **`100 → work_order` ממופה** (`b1c1d31`), והמבחן שהכריע נמדד בפרודקשן:
      שני רצפים **נעולים**, אחד על `current 1003`. מספר רגולטורי שהוקצה ואינו
      בקובץ הוא חור ברצף.
- [x] ⛔ **`proforma → 300` הוסר** (`7cb9ce6`) — נכשל באותו מבחן:
      `0` עמודי טופס, `0` form clients, `0` מסמכים אי פעם.
      **מיפוי בלי נתיב הנפקה הוא הצהרה על מה שהתוכנה לא עושה.**
- [x] **האינווריאנטה, מקובעת בטסט:** קוד ממופה חייב 10+ מסמכים בנתוני ההגשה,
      וסוג בנתונים חייב קוד. ממופה עכשיו: `305 · 320 · 400 · 100`.
- [ ] `330` — נכנס עם עבודת הזיכוי, לא לפניה

---

## שלב 2 — נתוני הגשה

**שני מגנים, שניהם אומתו בקוד ולא בהערה:**
- `issue_date >= 2026-01-01` הוא תנאי אמיתי — `document-helpers.ts:424`.
  כל נתוני הדגמה נושאים **תאריכי 2025**, ולכן שער ההקצאה לא נבחן.
- הסף הוא `5000` והוא **נכשל-בטוח** — `document-helpers.ts:434` חוזר ל-5000
  גם אם השורה במסד תיעלם. `subtotal` מקסימלי **3000**.

**תמהיל, יעד ≥3,000 רשומות ו-≥1.0MB:**

| קוד | סוג | רשומות למסמך | מסמכים | שורות | רשומות |
|---|---|---|---|---|---|
| `100` | הזמנה | `C100 + n×D110` | 130 | 2–10 | 910 |
| `305` | חשבונית מס | `C100 + n×D110` | 130 | 2–10 | 910 |
| `320` | חשבונית מס/קבלה | `C100 + n×D110 + D120` | 130 | 2–10 | 1,040 |
| `400` | קבלה | `C100 + D120` | 130 | — | 260 |
| | A100 + Z900 | | | | 2 |
| | | | **520** | | **≈3,122 ≈ 1.06MB** |

- [x] **חברת הדגמה נוצרה דרך נתיב ההרשמה של האפליקציה עצמה** — לא INSERT.
      `03236c00-3953-417f-a1c9-4e3a745af2b2` · `חברת הדגמה בלבד` · `123456782` ·
      `osek_murshe` (במכוון: `osek_patur` היה מספק את שער ההקצאה מהסיבה הלא נכונה)
- [x] ⛔ **האימות שהותנה, לפני הרחבה** — על המסמך הראשון:
      `requires_allocation_number=false` · `allocation_status=not_required` ·
      `allocation_number`/`shaam_error_id`/`invoice_decision_type` **ריקים** ·
      `shaam_events` לחברה = **0**. שער התאריך (2025) חוסם לפני שהסף נבדק בכלל.
- [~] **אצווה קטנה: 10 מהם הופקו, לא 12** — ⛔ **חסום על מגבלת המסלול החינמי.**
      `scripts/091` סופר `document_status='final'` לפי **`finalized_at`** בתוך תקופת
      החיוב, לא לפי `issue_date` — ולכן תיארוך אחורה לא עוזר. `plan_id='free'`,
      תקרה 10, ומהמסמך ה-11 והלאה: `limit_reached`, טיוטה נשארת, מספר נשרף.
      הופקו: `305 ×4 · 320 ×3 · 400 ×3 · 100 ×0`
- [x] **שתי צורות ה-320 קיימות בקובץ אחד** — מספר 2 ומספר 5 עם שורת תשלום,
      מספר 6 **בלי** (`C100 + 2×D110`, אפס D120). הטופס אינו יכול להפיק את הצורה
      הזאת בכלל — `allPaymentsConfirmed` דורש שורה מאושרת עם אמצעי וסכום > 0 —
      ולכן היא הופקה דרך הפעולה ישירות. הסימולטור יכריע.
- [ ] ⛔ **קוד 100 חסר לגמרי** — 5 טיוטות `work_order`, אפס סופיים, כולן על
      `limit_reached`
- [ ] סימולטור על האצווה הקטנה, ורק אם נקי — 520
- [x] **קבלה 400: `1221` ו-`1222` יוצאים מאופסים** — אומת בבייטים בשלוש הקבלות:
      `+00000000000000` בשניהם, ו-`1223` נושא את הסכום. הקובץ תמיד היה נכון.
      ⚠️ אבל **הבונה דיווח על שלושתן כאי-התאמת סכומים** (0+0≠1500), כלומר התראה
      שנדלקת על כל קבלה לנצח. תוקן ב-`af65d91` בצמצום: רק כששני המרכיבים אפס
      **וגם** הסוג הוא מסמך-תשלום. קבלה עם subtotal חלקי עדיין מדווחת.
- [ ] כל המסמכים דרך `issueDocumentAction` — הרנס מייבא אותה ישירות, עם זהות
      אמיתית של משתמש. **לא הזרקה למסד, ולא ידנית בדפדפן.**
- [x] **הופקו INI.TXT ו-BKMVDATA.TXT אמיתיים מהחברה הזאת**, לסקראץ' בלבד:
      `INI.TXT` 531 בייט · `BKMVDATA.TXT` 11,128 בייט · 33 רשומות ·
      `A100 1 · C100 10 · D110 15 · D120 6 · Z900 1` ·
      נתיב `OPENFRMT\12345678.26\08131523` · `1023=2025` · `1024=20250101` ·
      `1025=20251231` · `1026=20260813` · `1027=1523` — והשעה בדוח זהה ל-hhmm בנתיב
- [ ] ⛔ **הרצת הסימולטור עצמו היא צעד של איציק** — הסימולטור הוא כלי של רשות
      המסים ואינו בריפו. הקבצים מוכנים ב-
      `scratchpad/bkmv/demo.BKMVDATA.TXT` ו-`demo.INI.TXT`
- [ ] הגשה

---

## נסגר 13.8.2026 · על `regulatory/bkmv-spec`

| קומיט | מה | הפלט שמאחוריו |
|---|---|---|
| `419328b` | `1006 = 99999999`, `isSample` נגזר ממנו | 71 טסטים, גזירה מקובעת |
| `6640ec4` | `1265`/`1267` נטו · **`136-RECOVERED`** | `wc -c`: 23,534 / 25,655 בייט |
| `f5a29b3` | דוח 2.6 · 27 שורות · הדפסה · **כניסת תפריט** | בילד + route manifest |
| `b1c1d31` | `100 → work_order` | מדידת `document_sequences` בפרודקשן |
| `7cb9ce6` | `proforma` הוסר · האינווריאנטה · `FOLLOWUPS` | `0/0/0` נמדד |

**⛔ ו-136 אבדה מהריפו ושוחזרה:** שני הקומיטים שנגעו בה שמרו **0 בייטים**
(`53d51c9`, `c28e38b`), ועל הדיסק היא שרדה כתו טאב. ה-ROLLBACK שרד ב-18,834.
הסיבה: קומיט בלי מדידה אחריו, ו-`git add` בולע אפס בייטים בשקט.
**מהיום: `wc -c` על כל קובץ SQL אחרי כל קומיט. 0 או 1 בייט הוא כשל.**

**⚠️ ומה שאין לו וי:** `1006` ו-`1265/1267` **מיושמים אך טרם הופיעו בקובץ מופק** —
הקבצים שהפקנו קדמו להם. הווי יינתן כשאצווה חדשה תדגים אותם בבייטים.

---

## רשימה קפואה — לא נוגעים עד אחרי ההגשה

| נושא | הערה |
|---|---|
| באג הבעלות ב-`_impl` | כלל עומד: `/api/auditor/billing/renewals/run` לא מחובר לסקדיולר |
| מיגרציות 121/122 — ייחודיות ח.פ. | כתובות, לא הורצו |
| הפרדת Preview מ-Production | מסד אחד, מדוד. מיטיגציה: לא מנפיקים מדיפלוי Preview |
| תיקון 1157, 1000–1003, 4000 | final. לא מתוקנים, לא נמחקים. טיפול נפרד אחרי ההגשה |
| ריבוי עסקים תחת ח.פ. אחד | נדון, נדחה |
| סחיפת מיגרציות | |
| שדה 1231 — סניף/מחלקה | |
| `quote_literal(NULL)` ב-`customer_tax_id` | באג רדום בנתיב תשלום |
| 47 הפרות FK ב-`vow_billing_issued_documents` | |
| תיקונים חזותיים מול איציק ברלב | מסלול נפרד, לא חוסם |
| ⛔ `delivery_note` — רצף **נעול** לסוג שאין לו נתיב הנפקה | `FOLLOWUPS.md`. חייב להשתחרר או להימפות **לפני תעודת המשלוח הראשונה**. היום החור תיאורטי |
| שדה אימייל לקוח בטופס מסמך מס | עבודת מוצר |
| `meta=null` על שורת קבלה — המפַּה נופלת אחורה לפי סוג המסמך | עובד במקרה. יישבר כשקבלה תישא גם שורת פריט |
| באג הבעלות ב-`_impl` — הדו-ארגומנטי כותב את הקונה | מתועד ב-`136-RECOVERED.sql` |
