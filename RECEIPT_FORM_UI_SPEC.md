# SPEC: שינוי לוגיקה ו-UI במסך יצירת קבלה

## מטרה
שינוי מסך יצירת קבלה כך שיכלול 3 כפתורים עיקריים במקום הכפתורים הישנים, עם זרימות עבודה נפרדות לכל כפתור.

---

## 1. כפתורים במסך יצירה

### סדר כפתורים (RTL, מלמעלה למטה או משמאל לימין):
1. **"הפקת מסמך"** (Primary - ירוק)
2. **"שמירת טיוטה"** (Secondary - outline)
3. **"תצוגה מקדימה"** (Ghost/Outline)

### מצבים:
- **Loading:** כפתור disabled + spinner
- **Error:** Toast/הודעה
- **Success:** תלוי בכפתור (ראה זרימות)

---

## 2. כפתור: "תצוגה מקדימה"

### מטרה
הצגת PDF לצורך המחשה בלבד, עם Watermark "להמחשה בלבד" קבוע.

### התנהגות:
1. **ולידציה בסיסית:**
   - שם לקוח (חובה)
   - תאריך (חובה)
   - לפחות תקבול אחד (חובה)
   - אם חסר → הודעת שגיאה

2. **פתיחת מודאל:**
   - Overlay כהה ברקע
   - מודאל גדול במרכז המסך
   - כפתור X בפינה העליונה (סוגר מודאל, לא מאפס טופס)
   - לחיצה על Overlay יכולה לסגור (אופציונלי)

3. **תוכן מודאל:**
   - PDF מוצג (iframe/embed)
   - **Watermark CSS Overlay בלבד:** "להמחשה בלבד"
     - מיקום: מרכזי/אלכסוני
     - שקיפות: 0.3-0.5
     - לא ניתן למחיקה
     - תמיד מוצג בתצוגת Preview
     - **לא מוטבע ב-PDF עצמו**

4. **מצבים:**
   - **Loading:** Spinner + כפתור disabled
   - **Error:** הודעה "לא הצלחנו ליצור תצוגה מקדימה"
   - **Success:** PDF מוצג עם Watermark CSS overlay

### הערות:
- Preview הוא לצפייה בלבד
- אין כפתור הורדה מתוך המודאל
- אין יצירת מסמך רשמי
- **Watermark הוא CSS overlay בלבד (לא מוטבע ב-PDF)**
- **Final PDF ללא Watermark**

---

## 3. כפתור: "שמירת טיוטה"

### מטרה
שמירת טיוטה של הקבלה (Draft) כדי שהמשתמש יוכל לחזור אליה.

### התנהגות:
1. **ולידציה:**
   - מינימלית - טיוטה יכולה להישמר גם אם חסרים שדות

2. **שמירה:**
   - קריאה ל-`saveReceiptDraftAction` או `updateReceiptDraftAction`
   - Upsert לפי מצב קיים

3. **תוצאה:**
   - **Success:** Toast "הטיוטה נשמרה"
   - **Error:** Toast "שמירת טיוטה נכשלה"
   - **לא מפיק מסמך רשמי**
   - **לא פותח מודאל**
   - **נשאר במסך (ללא redirect)**

---

## 4. כפתור: "הפקת מסמך"

### מטרה
הפקת מסמך סופי וחוקי (Final) עם מספר, נעילה, ו-PDF.

### זרימה:

#### שלב 1: מודאל אישור
לחיצה על "הפקת מסמך" → פתיחת מודאל אישור:

**תוכן מודאל:**
- **כותרת:** "אישור הפקת חשבונית מס / קבלה {{NUMBER}}"
- **אזהרה (צהוב):** "רגע לפני שמאשרים שווה לעבור פעם נוספת על הפרטים ולוודא שהם נכונים, כי אחרי הפקת המסמך אי אפשר יהיה לתקן אותו."
- **טבלת נתונים:**
  - תאריך המסמך: {{DATE}}
  - שם הלקוח: {{CUSTOMER_NAME}}
  - סכום מסמך: {{AMOUNT}}
- **הערה על אימייל:** "רק אומרים שלא הזנת כתובת מייל לשליחת המסמך (אבל אפשר להפיק אותו ולשלוח אחר כך)"
- **כפתורים:**
  - "לאישור והפקה" (Primary - ירוק)
  - "חזרה לעריכה" (Secondary - outline)
- **כפתור X** לסגירה

#### שלב 2: הפקה סופית
לחיצה על "לאישור והפקה" → הפקה סופית:

1. **ולידציה מלאה** של כל שדות חובה
2. **קריאה ל-`issueReceiptAction`** שמבצע:
   - הקצאת מספר מסמך סופי (source number)
   - סימון המסמך כ-`status = final`
   - יצירת PDF (render template → pdf)
   - העלאה ל-Supabase Storage (Private bucket)
   - שמירת `pdf_storage_key` ו-metadata ב-DB
3. **Loading:** כפתור disabled + spinner
4. **Idempotency:** מניעת יצירה כפולה

#### שלב 3: מודאל הצלחה
אחרי הצלחה → פתיחת מודאל הצלחה:

**תוכן מודאל:**
- **אייקון ✓ ירוק** (גדול, מרכזי)
- **כותרת:** "חשבונית עסקה מספר {{DOCUMENT_NUMBER}} | {{BUSINESS_NAME}}"
- **כפתור X** בפינה שמאל-עליונה

**Grid פעולות (2 שורות, 3 עמודות):**

**שורה ראשונה:**
1. **צפייה בעמוד המסמך** (אייקון עין)
   - פעולה: ניווט ל-`/dashboard/documents/{id}`
2. **הורדת מסמך מקור** (אייקון הורדה)
   - פעולה: הורדת PDF סופי (Signed URL)
3. **הורדת העתק נאמן למקור** (אייקון מסמך/העתק)
   - פעולה: Placeholder / "בקרוב"

**שורה שנייה:**
4. **שיתוף ב-Telegram** (אייקון Telegram)
   - פעולה: Placeholder / "בקרוב"
5. **שיתוף ב-WhatsApp** (בולט - רקע ירוק בהיר)
   - פעולה: פתיחת WhatsApp share link עם הודעה + Signed URL
6. **העלאה ל-Google Drive** (אייקון Google Drive)
   - פעולה: Placeholder / "בקרוב" / disabled

**אזור הודעת אינפו (כחול בהיר):**
"המסמך הופק ובעוד מספר רגעים אפשרי לצפות בו. אם עברת לעמוד המסמכים והוא עדיין לא שם – כדאי לרפרש את הדפדפן."

**כפתורים תחתונים:**
- **"צפייה במסמך"** (Primary - ירוק)
  - פעולה: ניווט לעמוד המסמך הסופי
- **"סגירה"** (Secondary - outline)
  - פעולה: סגירת מודאל וחזרה למסך יצירה

---

## 5. Acceptance Criteria

### כפתורים:
- [ ] במסך יצירת קבלה מוצגים רק 3 כפתורים
- [ ] אין כפתור "יצירת קבלה" הישן
- [ ] סדר כפתורים: הפקת מסמך | שמירת טיוטה | תצוגה מקדימה

### תצוגה מקדימה:
- [ ] "תצוגה מקדימה" פותח מודאל עם PDF
- [ ] Watermark "להמחשה בלבד" מוצג (CSS overlay בלבד, לא מוטבע ב-PDF)
- [ ] יש כפתור X לסגירה
- [ ] סגירה מחזירה למסך בלי לאבד נתונים
- [ ] ולידציה בסיסית לפני פתיחת מודאל

### שמירת טיוטה:
- [ ] "שמירת טיוטה" שומרת Draft
- [ ] מציגה Toast הצלחה/שגיאה
- [ ] לא פותחת מודאל
- [ ] נשארת במסך (ללא redirect)

### הפקת מסמך:
- [ ] "הפקת מסמך" פותח מודאל אישור
- [ ] מודאל אישור כולל נתונים ואזהרה
- [ ] אחרי אישור → הפקה סופית (מספר, נעילה, PDF)
- [ ] אחרי הצלחה → מודאל הצלחה
- [ ] מודאל הצלחה כולל כל הפעולות (Grid 2x3)
- [ ] WhatsApp בולט (רקע ירוק)
- [ ] הורדת PDF עובדת (Signed URL)
- [ ] אין יצירה כפולה (idempotency)
- [ ] PDF סופי ללא Watermark

---

## 6. הערות טכניות

### Watermark:
- **Preview:** CSS overlay בלבד (לא מוטבע ב-PDF עצמו)
- **Final PDF:** ללא Watermark (PDF נקי)

### Storage:
- PDF נשמר ב-Private bucket
- הורדה באמצעות Signed URL
- אין Public URLs

### Idempotency:
- אם המסמך כבר issued → מחזיר אותו DOCUMENT_NUMBER
- מניעת מספור כפול
